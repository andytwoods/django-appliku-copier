#!/usr/bin/env bash
set -e
{% if web_server == "uvicorn" -%}
exec uvicorn config.asgi:application --host 0.0.0.0 --port 8000 --workers "${WEB_CONCURRENCY:-2}"
{%- else -%}
exec gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers "${WEB_CONCURRENCY:-2}"
{%- endif %}
