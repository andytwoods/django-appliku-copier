[project]
name = "testapp1"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "django>=4.2,<6",
    "gunicorn",
    "uvicorn[standard]",
    "whitenoise[brotli]",
    "django-environ",
    "psycopg2-binary",
]
